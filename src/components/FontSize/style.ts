import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderRadius: 10,
        borderWidth: 1,
        width: '80%',
        marginBottom: 20,
        padding: 10,
        alignItems: 'center',
    },


    title: {
        fontSize: 20,
        fontFamily: "google-sans",
    }
});
